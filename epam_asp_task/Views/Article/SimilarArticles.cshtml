@model Dictionary<int, string>

@if (Model.Count != 0)
{
    <div class="panel panel-default">
        <div class="panel-heading">
            <p>Similar</p>
        </div>
        <div class="panel-body">
            @foreach (var pair in Model)
            {
                @Html.ActionLink(pair.Value, "Article", "Article", new { articleId = pair.Key }, null)
                <br>
            }
        </div>
    </div>
}
